spring:
  application:
    name: collector
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
      fail-fast: true
    discovery:
      enabled: true
  config:
    import: optional:configserver:http://localhost:8888