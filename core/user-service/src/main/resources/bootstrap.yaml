spring:
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      retry:
        initial-interval: 5000
        max-interval: 30000
        multiplier: 2.0
        max-attempts: 10
      override-none: true
      override-system-properties: false